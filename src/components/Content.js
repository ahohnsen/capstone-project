import styled from 'styled-components';

const Content = styled.main`
  width: 100%;
  max-width: 500px;
  padding: 40px 0 20px;
  overflow-y: auto;
  overflow-x: hidden;
  margin: 0 auto;
`;

export default Content;
