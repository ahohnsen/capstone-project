import styled from 'styled-components';

const Content = styled.main`
  width: 100vw;
  max-width: 500px;
  padding: 40px 0 20px;
  overflow-y: auto;
  overflow-x: hidden;
`;

export default Content;
